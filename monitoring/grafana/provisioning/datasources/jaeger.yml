apiVersion: 1

datasources:
  - name: jaeger
    type: jaeger
    access: proxy
    url: http://jaeger:16686
    isDefault: false
    jsonData:
      # You can add additional Jaeger specific settings here if needed
      # e.g., queryTimeout: 30